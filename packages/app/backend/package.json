{
  "name": "@portfolio/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "serverless offline start",
    "deploy": "serverless deploy --stage production --aws-profile aws-kyler",
    "clean": "rm -rf node_modules && rm -rf dist && npm i && npx tsc --build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "serverless-http": "^3.2.0",
    "serverless-plugin-include-dependencies": "^6.1.1",
    "tsx": "^4.19.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.4",
    "serverless-offline": "^14.4.0",
    "typescript": "^5.7.3"
  }
}
